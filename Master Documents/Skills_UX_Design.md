# UX, Design & Content Skills — Master Catalog

These skills produce the elite, non-AI-slop assets and copy that ELITE Websites projects depend on. Invoke via the `Skill` tool.

## Frontend / UI Design

| Skill | When to Use |
|-------|-------------|
| `frontend-design:frontend-design` | **The flagship.** Build distinctive, production-grade frontend interfaces. Avoids generic AI aesthetics. Use whenever building components, pages, or apps. |
| `remotion` | Best practices for Remotion (React-based programmatic video). |

## Content & Copy (Instagram / Reels / Carousels)

| Skill | When to Use |
|-------|-------------|
| `caption-architect` | Full Instagram captions via Hook-Value-Story-CTA framework. |
| `hook-generator` | 10 ranked hook variations for Reels, carousels, captions. |
| `carousel-writer` | Slide-by-slide carousel copy, optimised for swipe-through + saves. |
| `reel-script` | Full Reel scripts (15s / 30s / 60s) with dialogue, on-screen text, pacing. |
| `comment-engine` | Strategic IG comments / replies / pinned comments for engagement growth. |
| `viral-hook-formula` | Reverse-engineer viral content; produce replicable formulas. |
| `trend-hijacker` | Adapt trends, viral formats, sounds, memes to a specific niche. |

## YouTube

| Skill | When to Use |
|-------|-------------|
| `youtube-script` | Long-form YouTube scripts optimised for watch time + retention. |
| `yt-title-thumbnail` | 10 high-CTR title variants + 3 detailed thumbnail briefs. |
| `yt-seo-description` | YouTube descriptions, chapter timestamps, tags, keyword strategy. |
| `faceless-channel-script` | Voiceover / AI-channel / animation scripts (no on-camera presence). |

## Video Production

| Skill | When to Use |
|-------|-------------|
| `viral-video-prompt` | Detailed AI video prompts (Runway, Sora, Kling, Pika, HeyGen). |
| `broll-shot-list` | Complete B-roll shot lists, scene directions, stock-footage search terms. |
| `short-form-repurpose` | Turn long-form (YouTube / podcast / blog) into 10+ short-form assets. |
| `opus-clip-automation` | Opus Clip configuration blocks for auto-editing + multi-platform export. |

## Storytelling & Strategy

| Skill | When to Use |
|-------|-------------|
| `emotional-storytelling` | Make content emotionally compelling — film/advertising techniques. |
| `story-arc-builder` | Narrative structures + story arcs for video, social, brand content. |
| `content-calendar` | 30 / 60 / 90-day content calendars across platforms. |

## Memory & Project Management

| Skill | When to Use |
|-------|-------------|
| `claude-mem:mem-search` | "Did we already solve this?" — search persistent cross-session memory. |
| `claude-mem:make-plan` | Detailed, phased implementation plan with doc discovery. |
| `claude-mem:do` | Execute a phased plan via subagents. |
| `claude-mem:smart-explore` | Token-optimised structural code search (tree-sitter AST). |
| `claude-mem:timeline-report` | "Journey Into [Project]" narrative report from claude-mem timeline. |

## Harness Configuration

| Skill | When to Use |
|-------|-------------|
| `update-config` | Configure settings.json — hooks, permissions, env vars. |
| `keybindings-help` | Customise `~/.claude/keybindings.json`. |
| `fewer-permission-prompts` | Build a smart allowlist from transcripts. |
| `simplify` | Review changed code for reuse / quality / efficiency, then fix. |
| `loop` | Run a prompt or slash command on a recurring interval. |
| `schedule` | Create / manage scheduled remote agents (cron). |
| `claude-api` | Build / debug / optimise Claude API + Anthropic SDK apps. |

## Telegram

| Skill | When to Use |
|-------|-------------|
| `telegram:configure` | Set up Telegram channel — bot token, access policy. |
| `telegram:access` | Manage Telegram pairings + allowlists. |
